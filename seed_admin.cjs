const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); const bcrypt = require('bcryptjs'); async function main() { const hash = await bcrypt.hash('123', 10); await prisma.admin.upsert({ where: { email: 'admin@gmail.com' }, update: { passwordHash: hash }, create: { email: 'admin@gmail.com', passwordHash: hash } }); console.log('Admin updated'); } main().then(() => prisma.$disconnect());
